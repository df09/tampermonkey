function searchActiveJobStart() {
  const operation = 'searchActiveJob/start';
  tmsSetOperation(operation);
  console.log(operation);
  tmMenu.showExec();

  // fetch data all active jobs
    // once per day
      // add refresh button
    // parse data
  // show modal-content
    // insert html
    // esc - close
    // inmplement filter by texting
      // enter:
        // if only one left: redirect
        // if more: by number row
}
