(function() {
  'use strict';

  function start() {
    // get list hw
    // login
  }

  // === action ============================================
  tmMenuAdd('Order hw');
  getEl(tmMenuStartSelector).addEventListener('click', () => start());
})();
